import React from 'react';
import './Button.css'; // Importa el archivo CSS para estilos del banner

const Button = () => {
    return (
        <div className="Button">
            <button className='Button-mas'>Ver Mas</button>
        </div>
    );
};

export default Button;